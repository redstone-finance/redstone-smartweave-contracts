import {ActionSchema, StateSchema} from "../schemas";
import {ContractResultSchema} from "../contract";

export function removeNetwork(state: StateSchema, action: ActionSchema): ContractResultSchema {
  // TODO: implement

  return {
    state,
    result: null
  };
}
